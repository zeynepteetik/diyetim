/*class WeightInfo {
  final int id;
  final String name;
  final Object weight;

  WeightInfo({required this.id, required this.name, required this.weight});

  Map<String, dynamic> toMap() {
    return {
      'id': id,
      'name': name,
      'date': weight,
    };
  }

  // Implement toString to make it easier to see information about
  // each dog when using the print statement.
  @override
  String toString() {
    return 'Weight{id: $id, name: $name, date: $weight}';
  }
}
*/
