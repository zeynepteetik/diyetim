/*class Person {
  int? id;
  String name;
  double weight;
  String date;

  Person({
    this.id,
    required this.name,
    required this.weight,
    required this.date,
  });

  Map<String, dynamic> toMap() {
    return {
      'id': id,
      'name': name,
      'weight': weight,
      'date': date,
    };
  }

  static Person fromMap(Map<String, dynamic> map) {
    return Person(
      id: map['id'],
      name: map['name'],
      weight: map['weight'],
      date: map['date'],
    );
  }
}
*/